version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
    volumes:
      - ./fitroom_fresh.db:/app/fitroom_fresh.db  # Optional: for persistent database
    restart: unless-stopped